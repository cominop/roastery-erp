Implement Step 25: Filter panel UI (collapsible panel, add/remove filters, clear all)

## Context

Step 24 (Filter state management — `useFilters` hook) is already complete. Create the visual filter panel that sits between the form navigation footer and the form content area, providing a collapsible UI for managing filters.

## Codebase Context

Working directory: /home/fcc-student/Desktop/_DEV/roastery-ui/

### Existing useFilters hook API (src/hooks/useFilters.ts):
```typescript
interface FilterItem {
  id: string;
  name: string;
  expression: string;
  active: boolean;
}

// Hook returns:
// filters: FilterItem[] — all registered filters
// activeFilters: FilterItem[] — only enabled filters
// combinedFilter: string | undefined — SQL WHERE clause from active + baseFilter (AND'd)
// hasActiveFilters: boolean
// addFilter(name, expression): string — returns generated id
// removeFilter(id): void
// toggleFilter(id): void
// setFilterActive(id, active): void
// clearFilters(): void
// updateFilter(id, updates): void
// setFilters(items): void
```

The hook is already exported from src/hooks/index.ts:
```typescript
export { useFilters } from "./useFilters";
export type { FilterItem, UseFiltersOptions, UseFiltersReturn } from "./useFilters";
```

### UI Components Available:
- src/components/ui/button.tsx (Button)
- src/components/ui/input.tsx (Input)
- src/components/ui/switch.tsx (Switch)
- src/components/ui/label.tsx (Label)
- src/components/ui/select.tsx (Select)
- src/components/ui/card.tsx (Card components)
- src/components/ui/badge.tsx (likely exists - need to check)

### Import pattern:
```typescript
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
```

Using `lucide-react` icons: Filter, Plus, X, ChevronDown, ChevronUp, Search, SlidersHorizontal

### Testing:
- Unit tests: vitest (test:unit script)
- E2E: Playwright (test script)
- Use @testing-library/react for component tests
- Tests live in __tests__/ next to the source file or in src/hooks/__tests__/

## What To Build

### Files to create:

1. **src/filters/FilterPanel.tsx** — Main filter panel component
   Props: useFilters return value (all hook methods and state)
   
   Collapsed state (spec section 6.1):
   ```
   ┌─ Filters ────────────────────────────────────┐
   │  [🔍 Search all fields...]  [3 active]  [▼]  │
   │                                               │
   │  Status: Active  |  Date: Jan-Jun 2026       │
   │  Customer: Contains "Smith"                   │
   └───────────────────────────────────────────────┘
   ```
   — Collapsed: shows a thin bar with filter count badge and toggle arrow
   — If filters are active, shows 1-line summary of active filters
   — Click the bar or arrow to expand
   
   Expanded state (spec section 6.2):
   ```
   ┌─ Filters ────────────────────────────────────┐
   │  Header: [Search input] [Clear All] [▲]      │
   │                                               │
   │  ┌─ Active Filters ───────────────────────┐  │
   │  │  Each filter: name, expression badge,   │  │
   │  │  active toggle (Switch), remove (X btn) │  │
   │  └────────────────────────────────────────┘  │
   │                                               │
   │  ┌─ Add Filter ───────────────────────────┐  │
   │  │  Name: [____________]                  │  │
   │  │  Expression: [____________]            │  │
   │  │  [Add Filter] button                   │  │
   │  └────────────────────────────────────────┘  │
   └──────────────────────────────────────────────┘
   ```
   — Search input to filter the displayed filter list by name
   — "Clear All" button (calls clearFilters, only shown when filters.length > 0)
   — Active filters list: each shows name, expression preview, toggle switch, remove button
   — "Add Filter" section at the bottom: name + expression inputs + add button
   — Collapse arrow to go back to collapsed state

2. **src/filters/FilterSummary.tsx** — Active filter summary chips
   Props: { activeFilters: FilterItem[], onToggle: (id) => void, onRemove: (id) => void }
   Shows compact chip/badge for each active filter with name and a small X to remove

3. **src/filters/index.ts** — Barrel export
   - Export FilterPanel, FilterSummary

4. **src/filters/__tests__/FilterPanel.test.tsx** — Unit tests for FilterPanel
   Tests:
   - Renders in collapsed state by default
   - Shows filter count badge when filters are active
   - Expands/collapses on click
   - Shows "Add Filter" form when expanded
   - Calls addFilter when add button clicked
   - Calls clearFilters when Clear All clicked
   - Calls removeFilter when remove X clicked on a filter
   - Calls toggleFilter when toggle switch clicked
   - Shows active filter summary in collapsed state

5. **src/filters/__tests__/FilterSummary.test.tsx** — Unit tests for FilterSummary
   Tests:
   - Renders active filters as chips
   - Shows filter names
   - Calls onRemove when X clicked
   - Shows nothing when empty

## Spec Reference

Full spec: /home/fcc-student/Documents/Obsidian Vaults/FCC Email Knowledge Base/Roastery ERP - SPEC - Filtering.md

Key sections:
- Section 5: Filter state types (already implemented in useFilters hook)
- Section 6: Filter Panel UI (collapsed and expanded states)

## Styling

Use Tailwind CSS (already configured). The project uses class-variance-authority and clsx for conditional classes. Look at existing components for styling patterns.

Primary colors used in project:
- Filter panel should have a subtle background (e.g., bg-muted/50 or border)
- Active filter badges use primary/blue tones
- Collapsed bar should have a small border-top to separate from form content

## Deliverable

Working code that:
1. Creates FilterPanel component that integrates with the existing useFilters hook
2. Creates FilterSummary component for inline filter badges
3. Both components are unit tested
4. Barrel export from src/filters/index.ts

Do NOT modify any existing files — only create new ones in src/filters/.

Run `cd /home/fcc-student/Desktop/_DEV/roastery-ui && npm run test:unit -- --run` after implementation to verify tests pass.
