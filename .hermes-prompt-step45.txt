Implement Step 45: Calculated field definition table + API

## Context
Steps 42-44 are complete:
- Step 42: Expression parser (tokenise → AST, safe no-eval, in `server/calculated_fields/expression_parser.py`)
- Step 43: Expression evaluator (AST → result with context, in `server/calculated_fields/expression_evaluator.py`)
- Step 44: Function library (SUM, COUNT, AVG, LOOKUP, IF, DATEDIFF, TODAY, NOW, CONCAT, UPPER/LOWER, ROUND, COALESCE — integrated into the evaluator)

## What to build

### 1. SQL migration: `server/sql/step-45-calculated-fields-table.sql`
Create a table `shared.calculated_fields` in the `polyaccess` database with these columns (matching the spec):

```sql
CREATE TABLE IF NOT EXISTS shared.calculated_fields (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  name VARCHAR(255) NOT NULL,                    -- snake_case identifier
  caption VARCHAR(255) NOT NULL,                 -- display label
  table_name VARCHAR(255) NOT NULL,              -- belongs to this db_fcc_erp table
  
  calc_type VARCHAR(20) NOT NULL CHECK (calc_type IN ('scalar', 'aggregate', 'lookup', 'formula', 'stored')),
  expression TEXT NOT NULL,                       -- the calculation expression
  data_type VARCHAR(20) NOT NULL CHECK (data_type IN ('text', 'number', 'currency', 'boolean', 'date')),
  
  depends_on TEXT[] DEFAULT '{}',                 -- field names this calc depends on
  depends_on_tables TEXT[] DEFAULT '{}',          -- related tables (for aggregates)
  
  read_only BOOLEAN DEFAULT true,                -- always read-only
  refresh_on VARCHAR(10) DEFAULT 'read' CHECK (refresh_on IN ('read', 'save', 'manual')),
  null_when_empty BOOLEAN DEFAULT false,
  
  format VARCHAR(100),                            -- e.g. "$%.2f", "%d items"
  decimals INTEGER,
  prefix VARCHAR(50),
  suffix VARCHAR(50),
  
  visible BOOLEAN DEFAULT true,
  sortable BOOLEAN DEFAULT true,
  filterable BOOLEAN DEFAULT false,
  
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW(),
  
  UNIQUE(name, table_name)
);
```

Include an index on `(table_name)`.

### 2. CRUD API endpoints in `server/index.cjs`
Add routes after the event handlers section (before `// ─── Event Engine — hierarchical dispatch chain`):

- `GET /api/calculated-fields` — list all, with optional `?table_name=` filter
- `GET /api/calculated-fields/:id` — get one by ID
- `POST /api/calculated-fields` — create new calculated field definition
- `PUT /api/calculated-fields/:id` — update
- `DELETE /api/calculated-fields/:id` — delete

All routes in the `shared.calculated_fields` table. Use the same pattern as the event handlers routes (pool.query with $1 params, async error handling, etc.).

### 3. TypeScript types: create `src/calculated-fields/schema/calculatedFieldSchema.ts`
Export the CalculatedField interface matching the spec:

```typescript
export interface CalculatedField {
  id: string;
  name: string;
  caption: string;
  tableName: string;
  calcType: 'scalar' | 'aggregate' | 'lookup' | 'formula' | 'stored';
  expression: string;
  dataType: 'text' | 'number' | 'currency' | 'boolean' | 'date';
  dependsOn: string[];
  dependsOnTables: string[];
  readOnly: boolean;
  refreshOn: 'read' | 'save' | 'manual';
  nullWhenEmpty: boolean;
  format?: string;
  decimals?: number;
  prefix?: string;
  suffix?: string;
  visible: boolean;
  sortable: boolean;
  filterable: boolean;
  createdAt: string;
  updatedAt: string;
}
```

### 4. Create the API client: `src/calculated-fields/api/calculatedFieldsApi.ts`
Export functions:
- `fetchCalculatedFields(tableName?: string): Promise<CalculatedField[]>`
- `fetchCalculatedField(id: string): Promise<CalculatedField>`
- `createCalculatedField(field: Omit<CalculatedField, 'id' | 'createdAt' | 'updatedAt'>): Promise<CalculatedField>`
- `updateCalculatedField(id: string, field: Partial<CalculatedField>): Promise<CalculatedField>`
- `deleteCalculatedField(id: string): Promise<void>`

### 5. Write unit tests
Create `src/__tests__/calculatedFieldApi.test.ts` with Vitest tests:
- Test fetching all calculated fields
- Test fetching with table_name filter
- Test creating a calculated field
- Test updating a calculated field
- Test deleting a calculated field

Use fetch mock for the API tests (vi.fn()).

### Important notes:
- Follow existing patterns: `server/index.cjs` uses Express + `pool.query()` with $1 params
- The `shared` schema is already in use (event_handlers table is there)
- No need to run the SQL migration manually — just create the file
- Use `import` syntax for TypeScript, matching existing codebase style
- Run `npx vitest run` after implementing to verify all tests pass
- Ensure TypeScript compiles: npx tsc --noEmit
- Do NOT modify the Python parser/evaluator — they're already complete

