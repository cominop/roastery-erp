Implement Step 44: Function library (SUM, COUNT, AVG, LOOKUP, IF, DATEDIFF, CONCAT, etc.)

## Context

Steps 42 (Expression parser) and 43 (Expression evaluator) are already complete in `src/lib/expressions.ts`. The file already has:
- Tokeniser/lexer
- Recursive descent parser (generates AST with types: literal, string, date, field-ref, binary-op, concat, not-op, and-op, or-op, call, aggregate)
- Evaluator with built-in functions: IIF, NZ, ISNULL, NOW, DATE, LEFT, RIGHT, MID, LEN, TRIM, UCASE, LCASE, INSTR, REPLACE, INT, ABS, VAL, ROUND, DATEADD, FORMAT
- Aggregates: SUM, COUNT, AVG, MIN, MAX (operating on `ctx.groupRecords` / `ctx.allRecords`)
- Format value function (currency, fixed, percent, standard, short date, etc.)
- Export: `evaluateExpression(exprString: string, context: ExprContext): unknown`
- LRU parse cache (Map, max 500 entries)

## What Step 44 needs to add

From the spec (Roastery ERP - SPEC - Calculated Fields.md), the following functions are expected but NOT yet implemented:

1. **LOOKUP(tableAndField: string, keyField: any) -> any**
   - Lookup a value from a related table
   - Syntax: `LOOKUP(customers.name, {customer_id})`
   - Parses `customers.name` into table=tableName and field=fieldName
   - Uses `ctx.databaseLookup(table, field, keyValue)` from the ExprContext to find the value
   - Returns the looked-up value or null if not found

2. **DATEDIFF(unit: string, date1: any, date2: any) -> number**
   - Calculate difference between two dates
   - Syntax: `DATEDIFF('day', {start}, {end})`
   - Supports units: 'day'/'d', 'month'/'m', 'year'/'yyyy', 'hour'/'h', 'minute'/'n', 'second'/'s'
   - Returns integer count

3. **TODAY() -> Date**
   - Alias for DATE() / NOW() — returns current date

4. **CONCAT(...values: any[]) -> string**
   - Variadic string concatenation
   - Syntax: `CONCAT({first}, " ", {last})`
   - Joins all arguments as strings, returns empty string if no args

5. **COALESCE(...values: any[]) -> any**
   - Return first non-null value
   - Syntax: `COALESCE({phone}, {mobile}, "N/A")`
   - Returns null if all args are null

6. **IF(condition: any, trueVal: any, falseVal?: any) -> any**
   - Alias for IIF
   - Syntax: `IF({status}=1, "Active", "Inactive")`
   - Same implementation as IIF

7. **UPPER(str: any) -> string**
   - Alias for UCASE
   
8. **LOWER(str: any) -> string**
   - Alias for LCASE

## Implementation Details

### 1. Add `databaseLookup` to ExprContext type

The `ExprContext` type in `src/types.ts` needs a new optional field:
```typescript
databaseLookup?: (table: string, field: string, keyValue: unknown) => unknown | Promise<unknown>;
```

### 2. Update the ExprContext type definition

Find `src/types.ts` and update the `ExprContext` interface:
```typescript
export interface ExprContext {
  record?: Record<string, unknown>;
  groupRecords?: Record<string, unknown>[];
  allRecords?: Record<string, unknown>[];
  databaseLookup?: (tableName: string, fieldName: string, keyValue: unknown) => unknown;
}
```

### 3. Add the missing functions in `callFunction()` in `src/lib/expressions.ts`

Add new cases to the switch statement in `callFunction()`:

```typescript
case "if":
  // Alias for IIF
  return truthy(evaled[0]) ? evaled[1] : (evaled[2] ?? null);

case "lookup": {
  // LOOKUP(tableAndField, key)
  // Parse "customers.name" into table="customers", field="name"
  const tableAndField = toString(evaled[0]);
  const keyValue = evaled[1];
  const dotIdx = tableAndField.indexOf(".");
  if (dotIdx === -1) return null;
  const tableName = tableAndField.slice(0, dotIdx);
  const fieldName = tableAndField.slice(dotIdx + 1);
  if (ctx.databaseLookup) {
    return ctx.databaseLookup(tableName, fieldName, keyValue);
  }
  return null;
}

case "datediff": {
  const unit = toString(evaled[0]).toLowerCase().trim();
  const d1 = evaled[1] instanceof Date ? evaled[1] : new Date(toString(evaled[1]));
  const d2 = evaled[2] instanceof Date ? evaled[2] : new Date(toString(evaled[2]));
  const diffMs = d2.getTime() - d1.getTime();
  switch (unit) {
    case "d":
    case "day":
    case "days":
      return Math.round(diffMs / 86400000);
    case "h":
    case "hour":
    case "hours":
      return Math.round(diffMs / 3600000);
    case "n":
    case "minute":
    case "minutes":
      return Math.round(diffMs / 60000);
    case "s":
    case "second":
    case "seconds":
      return Math.round(diffMs / 1000);
    case "m":
    case "month":
    case "months": {
      let months = (d2.getFullYear() - d1.getFullYear()) * 12;
      months += d2.getMonth() - d1.getMonth();
      return months;
    }
    case "y":
    case "yyyy":
    case "year":
    case "years":
      return d2.getFullYear() - d1.getFullYear();
    default:
      return null;
  }
}

case "today":
  return new Date();

case "concat":
  return evaled.map((v) => toString(v)).join("");

case "coalesce":
  for (const v of evaled) {
    if (v != null && v !== "") return v;
  }
  return null;

case "upper":
  return toString(evaled[0]).toUpperCase();

case "lower":
  return toString(evaled[0]).toLowerCase();
```

### 4. Write unit tests

Add tests to `src/lib/__tests__/expressions.test.ts`:

```typescript
describe("DATEDIFF", () => {
  it("calculates day difference", () => {
    expect(evalWith('DATEDIFF(\"day\", #01/01/2024#, #01/11/2024#)')).toBe(10);
  });
  it("calculates month difference", () => {
    expect(evalWith('DATEDIFF(\"month\", #01/01/2024#, #03/01/2024#)')).toBe(2);
  });
  it("calculates year difference", () => {
    expect(evalWith('DATEDIFF(\"yyyy\", #01/01/2024#, #01/01/2026#)')).toBe(2);
  });
  it("calculates hour difference", () => {
    const d1 = new Date("2024-01-01T00:00:00");
    const d2 = new Date("2024-01-01T05:30:00");
    expect(evalWithCtx('DATEDIFF(\"h\", #01/01/2024 00:00#, #01/01/2024 05:30#)', {})).toBe(6); // rounds
    // Actually let's use the helper
  });
});

describe("LOOKUP", () => {
  it("returns looked-up value from context", () => {
    const ctx: ExprContext = {
      record: { customer_id: 5 },
      databaseLookup: (table, field, key) => {
        if (table === "customers" && field === "name" && key === 5) return "Acme Corp";
        return null;
      },
    };
    expect(evaluateExpression('LOOKUP(customers.name, {customer_id})', ctx)).toBe("Acme Corp");
  });

  it("returns null when databaseLookup is not provided", () => {
    expect(evalWith('LOOKUP(customers.name, 5)')).toBeNull();
  });
});

describe("TODAY", () => {
  it("returns a Date", () => {
    const d = evalWith("TODAY()") as Date;
    expect(d).toBeInstanceOf(Date);
  });
});

describe("CONCAT", () => {
  it("concatenates multiple values", () => {
    const record = { first: "John", last: "Doe" };
    expect(evalWith('CONCAT([first], \" \", [last])', record)).toBe("John Doe");
  });
  it("handles single argument", () => {
    expect(evalWith('CONCAT(\"hello\")')).toBe("hello");
  });
  it("handles empty args", () => {
    // This is a syntax error actually — let's just test basic usage
  });
});

describe("COALESCE", () => {
  it("returns first non-null value", () => {
    expect(evalWith('COALESCE(Null, \"fallback\")')).toBe("fallback");
  });
  it("returns null when all are null", () => {
    expect(evalWith('COALESCE(Null, Null)')).toBeNull();
  });
  it("returns first value when all are non-null", () => {
    expect(evalWith('COALESCE(\"first\", \"second\")')).toBe("first");
  });
});

describe("IF (alias for IIF)", () => {
  it("returns true-part when condition is truthy", () => {
    expect(evalWith('IF(5 > 3, \"yes\", \"no\")')).toBe("yes");
  });
  it("returns false-part when condition is falsy", () => {
    expect(evalWith('IF(0, \"yes\", \"no\")')).toBe("no");
  });
});

describe("UPPER / LOWER", () => {
  it("UPPER converts to uppercase", () => {
    expect(evalWith('UPPER(\"hello\")')).toBe("HELLO");
  });
  it("LOWER converts to lowercase", () => {
    expect(evalWith('LOWER(\"HELLO\")')).toBe("hello");
  });
});
```

## File Changes Summary

1. **`src/types.ts`** — Add `databaseLookup` to `ExprContext`
2. **`src/lib/expressions.ts`** — Add cases in `callFunction()` for: `if`, `lookup`, `datediff`, `today`, `concat`, `coalesce`, `upper`, `lower`
3. **`src/lib/__tests__/expressions.test.ts`** — Add test blocks for each new function

## Working Directory

All work is in `/home/fcc-student/Desktop/_DEV/roastery-ui/`

## Verification

After implementing, run: `npx vitest run src/lib/__tests__/expressions.test.ts` and confirm all tests pass.
