Implement Step 63: Navigation Tree 3 — Collapsible Groups + Fuzzy Search

The SidebarTree component (`src/components/SidebarTree.tsx`) already has collapsible groups (NavGroup with open/close toggle based on is_expanded). The missing piece is the fuzzy search/filtering.

## What needs to be done

### 1. Add a search input at the top of the sidebar
In `SidebarTree.tsx`, add a search input between the "☕ Roastery ERP" header and the scrollable `<nav>` element:
- Use a text `<input>` with a search icon (Search from lucide-react)
- Placeholder: "Search navigation..."
- Small, compact styling: 10-11px font, minimal padding, fits the sidebar width
- The input should be in a sticky header area (non-scrolling)

### 2. Implement fuzzy filtering of the tree
- Add a `searchQuery` state (useState, string)
- Filter `rootNodes` based on the search query using a case-insensitive substring match on `node.label`
- When filtering:
  - If a group node (target_type === "group") has any matching children, show the group expanded
  - If a leaf node (table/form/report/link) matches, show it
  - If search is empty, show all nodes normally
- Use `useMemo` for the filtered tree to avoid re-computing on every render

### 3. Show/hide behavior
- When the search input is empty: show all groups with their normal collapsed/expanded state (is_expanded from the API data)
- When the search input has text: auto-expand any group that has matching children, hide non-matching nodes entirely

### 4. Props change
The SidebarTree component takes `tree: NavTreeNode[]` as a prop. The filtering is fully internal — no prop changes needed.

### 5. Types
Import `Search` from lucide-react for the search icon.

## Codebase context
- Working directory: /home/fcc-student/Desktop/_DEV/roastery-ui
- Component: src/components/SidebarTree.tsx (326 lines, already created by Step 62)
- Uses: lucide-react, React hooks (useState, useMemo, useCallback already imported)
- Import pattern: lucide icons are imported as named exports, e.g. `import { Search, Menu } from "lucide-react"`
- Badge styling pattern exists: `ml-auto text-[10px] px-1 py-0.5 rounded bg-muted-foreground/10 text-muted-foreground tabular-nums`

## Do NOT
- Do not change the API (server/index.cjs)
- Do not modify the App.tsx SidebarTree usage
- Do not modify any type definitions (NavTreeNode, ActiveView, SidebarTreeProps)
- Do not add new dependencies
- Do not change the test file (e2e/smoke.spec.ts)