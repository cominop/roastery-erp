# Step 52: Audit Trail 1 — `audit_log` table + indexes

## Context
This is Step 52 (Audit Trail, Step 1 of 9) of the Roastery ERP project. You already have:

- Full CRUD API at `server/index.cjs` with GET/POST/PUT/DELETE `/api/data/:table` routes
- PostgreSQL pool connecting to `polyaccess` database (shared schema for system tables, `db_fcc_erp` for ERP data)
- Existing system tables in `shared.` schema: `shared.objects`, `shared.event_handlers`, `shared.calculated_fields`, `shared.calculated_field_values`
- Permissions middleware at `server/permission-middleware.cjs`
- Event engine at `server/event-engine.cjs`
- Dev servers: API on port 3001, Vite on port 5173

## What to Build

### 1. SQL Migration (`server/sql/step-52-audit-log-table.sql`)

Create `shared.audit_log` table:

```sql
BEGIN;

CREATE TABLE IF NOT EXISTS shared.audit_log (
  id          UUID PRIMARY KEY DEFAULT gen_random_uuid(),

  -- What was changed
  table_name  VARCHAR(255) NOT NULL,
  record_id   INTEGER      NOT NULL,

  -- What action
  action      VARCHAR(10)  NOT NULL CHECK (action IN ('INSERT', 'UPDATE', 'DELETE')),

  -- Before/after snapshots (JSONB for flexible diffing)
  old_data    JSONB,
  new_data    JSONB,

  -- Who did it (foreign key to db_fcc_erp."Users" or just store the ID)
  changed_by  INTEGER,
  changed_by_name VARCHAR(255),

  -- When
  changed_at  TIMESTAMPTZ DEFAULT NOW(),

  -- Tenant scoping (matches existing company_id=1 pattern)
  company_id  INTEGER      NOT NULL DEFAULT 1
);

-- Index for lookup by table + record (most common query pattern)
CREATE INDEX IF NOT EXISTS idx_audit_log_record
  ON shared.audit_log (table_name, record_id);

-- Index for time-based queries (recent changes, date range filtering)
CREATE INDEX IF NOT EXISTS idx_audit_log_changed_at
  ON shared.audit_log (changed_at DESC);

-- Index for action-based filtering
CREATE INDEX IF NOT EXISTS idx_audit_log_action
  ON shared.audit_log (action);

-- Composite index for full-page listing (filter by table + date range)
CREATE INDEX IF NOT EXISTS idx_audit_log_table_date
  ON shared.audit_log (table_name, changed_at DESC);

-- Index for user-based queries
CREATE INDEX IF NOT EXISTS idx_audit_log_changed_by
  ON shared.audit_log (changed_by);

COMMIT;
```

### 2. Audit Log API Endpoints in `server/index.cjs`

Add the following routes after the existing calculated-fields routes (around line 1986, before the event engine mount):

**GET /api/audit-log — list audit log entries with filtering**
- Query params: `?table_name=`, `?action=`, `?changed_by=`, `?from=`, `?to=`, `?page=1`, `?limit=50`
- Returns paginated results with total count
- Ordered by `changed_at DESC`

**GET /api/audit-log/:id — single audit log entry**

### 3. Integrate Audit Logging into CRUD Routes

Hook into the existing POST/PUT/DELETE `/api/data/:table` routes to log changes:

**After POST /api/data/:table (line 344, after insert succeeds):**
```javascript
// Fire-and-forget audit log insert (best-effort, don't fail the save)
try {
  await pool.query(
    `INSERT INTO shared.audit_log (table_name, record_id, action, new_data, changed_by, changed_by_name)
     VALUES ($1, $2, 'INSERT', $3::jsonb, $4, $5)`,
    [table, saved[pkCol] || saved.id, JSON.stringify(saved), req.user?.id || null, req.user?.name || 'system']
  );
} catch { /* best-effort */ }
```

**After PUT /api/data/:table/:id (line 393, before returning saved):**
- Log the OLD data (fetch before updating) and the NEW data
- Fire-and-forget

**Before DELETE /api/data/:table/:id (line 427, before the delete):**
- Fetch the record first, then delete, log the old data
- Fire-and-forget

IMPORTANT: Keep the audit logging fire-and-forget (wrapped in try/catch) so it never blocks the main operation. The existing stored calc auto-computation shows this pattern.

**User identification:** For now, since the system uses `company_id: 1` hardcoded, set `changed_by = null` and `changed_by_name = 'system'`. The upcoming auth system will populate this.

### 4. Tests (`server/__tests__/test_audit_log.cjs`)

Create a simple test file that:
1. Verifies the SQL migration can run (syntax check via `node -e "require('pg')"`)
2. Verifies the audit log table exists (via pool.query)
3. Tests that POST creates an audit entry
4. Tests that PUT creates an audit entry
5. Tests that DELETE creates an audit entry
6. Tests GET /api/audit-log returns entries
7. Tests filtering by table_name

Use the existing test patterns from the codebase. Mock the DB or use a minimal test approach.

## Key Files for Reference
- `server/index.cjs` — main server file, existing CRUD routes at lines 327-438, stored calc pattern at lines 346-366
- `server/sql/step-51-stored-calc-values.sql` — existing migration for reference
- `server/permission-cache.cjs` — existing cache module pattern

## Delivery
When done, run:
```bash
cd /home/fcc-student/Desktop/_DEV/roastery-ui && node -e "require('./server/index.cjs')" 2>&1 | head -1
```

Also verify the SQL is valid by running a syntax check:
```bash
node -e "
const fs = require('fs');
const sql = fs.readFileSync('server/sql/step-52-audit-log-table.sql', 'utf8');
console.log('SQL file readable, length:', sql.length, 'bytes');
"
```

Then run:
```bash
cd /home/fcc-student/Desktop/_DEV/roastery-ui && npx vitest run 2>&1 | tail -10
```

All existing tests must still pass. The new audit log table migration should be syntactically valid PostgreSQL.
